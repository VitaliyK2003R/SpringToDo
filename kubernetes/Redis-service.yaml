apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    name: redis-service
    type: backend
spec:
  ports:
    - port: 6379
      targetPort: 6379
      nodePort: 30010
  selector:
    name: redis-pod
    type: backend