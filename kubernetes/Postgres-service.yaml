apiVersion: v1
kind: Service
metadata:
  name: postgres
  labels:
    name: postgres-service
    type: backend
spec:
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    name: postgres-pod
    type: backend